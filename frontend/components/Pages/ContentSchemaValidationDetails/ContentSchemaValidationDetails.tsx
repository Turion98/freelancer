"use client";

import React from "react";
import s from "./ContentSchemaValidationDetails.module.scss";

export function ContentSchemaValidationDetails() {
  return (
    <section id="content-schema-validation" className={s.wrapper}>
      <header className={s.hero}>
        <h2>Content Schema &amp; Validation System</h2>
        <p className={s.lead}>Subpage scaffold – content coming later.</p>
      </header>
    </section>
  );
}
